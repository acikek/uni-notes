#import "../../math-lecture-template.typ": *

#let template = base-template.with(color: colors.purple)

#let int = math.op("int")

#let open(space) = $op("open")_space$

#let closed(space) = $op("closed")_space$

#let adherent(space) = $op("adherent")_space$
